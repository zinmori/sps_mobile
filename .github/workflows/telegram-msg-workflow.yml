name: telegram message

on:
  push:
    branches:
      - main
      
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: send telegram message on push
        uses: appleboy/telegram-action@master
        with:
          to: 5080166932
          token: 6686189316:AAE5mWDgtRDaQPjDYX2OcQOC0JjveAoNYxs
          message: |
            ${{ github.actor }} a fait un push:
            Message de commit: ${{ github.event.commits[0].message }}
            
            Repository: ${{ github.repository }}
            
            Voir les changements: https://github.com/${{ github.repository }}/commit/${{github.sha}}
